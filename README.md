# Price Inference Microservice

I've built this simple service project so that future complex ML microservices could use this structured microservice approach. This microservice is local with end-to-end Continuous Integration. I've removed the training file, however when it's added, it even builds the model automatically when not present.
